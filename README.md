# Money Tracker – บันทึกรายรับรายจ่าย

> เว็บแอปบันทึกรายรับรายจ่ายแบบ Full-Stack ด้วย HTML, CSS, JavaScript และ Supabase

---

## 📋 สารบัญ

- [ภาพรวมโปรเจกต์](#ภาพรวมโปรเจกต์)
- [ฟีเจอร์หลัก](#ฟีเจอร์หลัก)
- [โครงสร้างไฟล์](#โครงสร้างไฟล์)
- [การติดตั้งและตั้งค่า](#การติดตั้งและตั้งค่า)
- [การ Deploy บน Render](#การ-deploy-บน-render)
- [สถาปัตยกรรมระบบ](#สถาปัตยกรรมระบบ)
- [การใช้งาน](#การใช้งาน)
- [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## ภาพรวมโปรเจกต์

**Money Tracker** เป็นเว็บแอปพลิเคชันสำหรับบันทึกรายรับรายจ่ายที่:

- ✅ **ระบบ Authentication**: Login/Register ด้วย Supabase Auth
- ✅ **Multi-User**: แต่ละผู้ใช้มีข้อมูลแยกกัน
- ✅ **Cloud Storage**: ข้อมูลเก็บใน Supabase Database
- ✅ **Responsive Design**: ใช้งานได้ทั้ง Desktop และ Mobile
- ✅ **Multi-Language**: รองรับภาษาไทยและอังกฤษ
- ✅ **Dark Mode**: รองรับ Light/Dark Theme

### เทคโนโลยีที่ใช้

- **Frontend**: HTML5, CSS3, Vanilla JavaScript (ES6+)
- **Backend**: Supabase (PostgreSQL + Authentication)
- **Deployment**: Render (Static Site)

---

## ฟีเจอร์หลัก

### 1. ระบบ Authentication
- สมัครสมาชิกด้วยอีเมลและรหัสผ่าน
- เข้าสู่ระบบ/ออกจากระบบ
- ข้อมูลผู้ใช้แต่ละคนแยกกันอย่างสมบูรณ์

### 2. บันทึกรายรับ/รายจ่าย
- เพิ่มรายการพร้อมประเภท (รายรับ/รายจ่าย)
- หมวดหมู่ที่กำหนดเอง
- วันที่, โน้ต, และเลือกบัญชีปลายทาง
- อัปเดตยอดคงเหลือบัญชีแบบเรียลไทม์

### 3. จัดการหลายบัญชี
- สร้างบัญชีหลายบัญชี (เงินสด, ธนาคาร, กระเป๋า ฯลฯ)
- แสดงยอดคงเหลือรายบัญชี
- โอนเงินระหว่างบัญชี (บันทึกเป็น 2 รายการอัตโนมัติ)
- เลือกสีสำหรับแต่ละบัญชี

### 4. หมวดหมู่ & งบประมาณรายเดือน
- จัดการหมวดหมู่รายรับ/รายจ่าย
- ตั้งงบประมาณต่อหมวด
- แถบความคืบหน้าการใช้งบ
- สถานะเตือนเมื่อใช้งบ 80%/100%

### 5. เป้าหมายการออม (Savings Goals)
- ตั้งเป้าหมายการออมเงิน
- ผูกกับบัญชีที่เลือก
- แสดง % ความคืบหน้า
- จำนวนวันคงเหลือ

### 6. สถิติ & แผนภูมิ
- กราฟรายจ่ายตามหมวด (Bar Chart)
- กราฟรายสัปดาห์ (Weekly Chart)
- กราฟรายเดือน (Monthly Chart)
- เปรียบเทียบรายรับ/รายจ่าย

### 7. ชาเลนจ์ & มินิเกม "ดวงไฟการออม"
- ภารกิจไม่ใช้จ่าย/ออมรายวัน
- หลอดไฟสว่างตามจำนวนวันที่ออมถึงเป้า
- สร้างแรงจูงใจในการออมเงิน

### 8. การยืม/ให้ยืม (Lending)
- บันทึกยอดที่ยืม/ปล่อยกู้
- วันที่ครบกำหนดชำระ
- ยอดชำระและสถานะ (รอ/ครบ/เกินกำหนด)
- ติดตามการชำระคืน

### 9. ค่าใช้จ่ายรายวัน
- ตั้งงบค่าใช้จ่ายรายวัน
- ติดตามค่าใช้จ่ายประจำวัน
- แจ้งเตือนเมื่อใกล้เกินงบ

### 10. ส่งออก/นำเข้า CSV
- แบ็กอัปข้อมูลเป็น CSV
- กู้คืนข้อมูลจาก CSV
- ย้ายข้อมูลระหว่างอุปกรณ์

### 11. ประสบการณ์ใช้งาน (UX)
- ซ่อนยอดรวม (ความเป็นส่วนตัว)
- ปุ่มกดด่วน (Quick Actions)
- โมดัลใช้งานง่าย
- แจ้งเตือนแบบ Real-time

---

## โครงสร้างไฟล์

```
MoneyTrackerProject/
├── index.html              # โครงหน้าเว็บ, Login/Register, และโมดัลต่าง ๆ
├── styles.css              # สี, ธีม, เลย์เอาต์, การ์ด, ปุ่ม, แผนภูมิ, โมดัล
├── app.js                  # แกนลอจิกทั้งหมด + Authentication + การจัดการข้อมูล
├── config.js               # การตั้งค่า Supabase (URL และ Anon Key)
├── supabase_schema.sql     # SQL Schema สำหรับสร้างตารางใน Supabase
├── render.yaml             # Render Blueprint Configuration (optional)
├── .gitignore              # Git ignore rules
└── README.md               # เอกสารนี้
```

---

## การติดตั้งและตั้งค่า

### 1. Clone หรือ Download โปรเจกต์

```bash
git clone <repository-url>
cd MoneyTrackerProject
```

### 2. ตั้งค่า Supabase

#### 2.1 สร้างโปรเจกต์ Supabase

1. ไปที่ [Supabase Dashboard](https://supabase.com/dashboard)
2. สร้างโปรเจกต์ใหม่
3. รอให้โปรเจกต์สร้างเสร็จ (ประมาณ 2-3 นาที)

#### 2.2 สร้างตารางในฐานข้อมูล

1. ใน Supabase Dashboard ไปที่ **SQL Editor** (เมนูด้านซ้าย)
2. เปิดไฟล์ `supabase_schema.sql` จากโปรเจกต์
3. คัดลอกเนื้อหาทั้งหมดและวางลงใน SQL Editor
4. กด **Run** เพื่อสร้างตารางและ policies

**สิ่งที่ SQL Schema จะสร้าง:**
- ตาราง `user_data` สำหรับเก็บข้อมูลผู้ใช้
- Row Level Security (RLS) policies
- Indexes สำหรับประสิทธิภาพ
- Trigger สำหรับอัปเดต `updated_at` อัตโนมัติ

#### 2.3 ตั้งค่า API Keys

1. ใน Supabase Dashboard ไปที่ **Settings** > **API**
2. คัดลอก **Project URL** (เช่น: `https://xxxxx.supabase.co`)
3. คัดลอก **anon public** key (จะเริ่มต้นด้วย `eyJ...`)
4. เปิดไฟล์ `config.js` ในโปรเจกต์
5. แก้ไขค่าตามนี้:

```javascript
const SUPABASE_CONFIG = {
    url: 'https://xxxxx.supabase.co',  // ใส่ Project URL ของคุณ
    anonKey: 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...'  // ใส่ anon key ของคุณ
};
```

#### 2.4 ตั้งค่า Authentication

1. ใน Supabase Dashboard ไปที่ **Authentication** > **Providers**
2. ตรวจสอบว่า **Email** provider เปิดใช้งานอยู่
3. (Optional) ปิดการยืนยันอีเมล:
   - ไปที่ **Authentication** > **Settings** > **Email Auth**
   - ปิด "Enable email confirmations" หากต้องการให้ผู้ใช้เข้าสู่ระบบได้ทันทีโดยไม่ต้องยืนยันอีเมล

### 3. ทดสอบการทำงาน

1. เปิดไฟล์ `index.html` ในเบราว์เซอร์ (Chrome แนะนำ)
2. คุณควรเห็นหน้า Login/Register
3. สมัครสมาชิกด้วยอีเมลและรหัสผ่าน
4. หลังจากสมัครสำเร็จ คุณจะเข้าสู่ระบบและสามารถใช้งานแอปได้

---

## การ Deploy บน Render

### สำหรับโปรเจกต์ใหม่

#### 1. เตรียมโปรเจกต์

1. สร้าง GitHub repository และ push โค้ดทั้งหมดขึ้นไป
2. ตรวจสอบว่าไฟล์ทั้งหมดอยู่ใน repository

#### 2. สร้าง Static Site บน Render

1. เข้าไปที่ [Render Dashboard](https://dashboard.render.com/)
2. คลิก **New +** > **Static Site**
3. เชื่อมต่อกับ GitHub repository ของคุณ
4. ตั้งค่าดังนี้:
   - **Name**: `money-tracker` (หรือชื่อที่คุณต้องการ)
   - **Build Command**: (เว้นว่างไว้)
   - **Publish Directory**: `.` (root directory)
   - **Branch**: `main` (หรือ branch ที่คุณใช้)

#### 3. Deploy

1. Render จะทำ Auto-Deploy อัตโนมัติเมื่อคุณ push โค้ด
2. หรือคลิก **Manual Deploy** > **Deploy latest commit**
3. รอให้ deployment เสร็จสิ้น
4. ทดสอบการทำงานของแอป

### สำหรับโปรเจกต์ที่มีอยู่แล้ว

**✅ ข่าวดี: ไม่ต้อง Config อะไรเพิ่มใน Render!**

**เหตุผล:**
- Render Static Site ไม่รองรับ Environment Variables สำหรับ Static Site
- ไฟล์ `config.js` มีค่าตั้งค่า Supabase อยู่แล้ว
- ระบบจะใช้ค่าจาก `config.js` โดยตรง

**สิ่งที่ต้องทำ:**

1. **Push โค้ดใหม่ขึ้น GitHub (Main branch)**
   ```bash
   git add .
   git commit -m "Add Supabase authentication"
   git push origin main
   ```

2. **Render จะ Auto-Deploy อัตโนมัติ**
   - เมื่อคุณ push ไปที่ Main branch
   - Render จะ detect การเปลี่ยนแปลงและ deploy อัตโนมัติ

3. **ตั้งค่า Supabase Database** (สำคัญ!)
   - ไปที่ Supabase Dashboard > SQL Editor
   - รันไฟล์ `supabase_schema.sql`
   - **ถ้าไม่ทำขั้นตอนนี้ ผู้ใช้จะไม่สามารถสมัครสมาชิกได้**

4. **ทดสอบหลัง Deploy**
   - ไปที่ URL ของเว็บไซต์บน Render
   - ทดสอบการสมัครสมาชิกและเข้าสู่ระบบ

**หมายเหตุ:**
- ❌ ไม่ต้องตั้งค่า Environment Variables ใน Render
- ❌ ไม่ต้องเปลี่ยน Build Command
- ❌ ไม่ต้องเปลี่ยน Publish Directory

---

## สถาปัตยกรรมระบบ

### Frontend Architecture

```
index.html
├── Auth Screen (Login/Register)
└── App Content
    ├── Header (Navigation)
    ├── Main Content
    │   ├── Dashboard View
    │   ├── Transactions View
    │   ├── Statistics View
    │   └── Accounts View
    └── Modals (Forms)
```

### Backend Architecture (Supabase)

```
Supabase
├── Authentication (auth.users)
│   └── Email/Password Authentication
└── Database (PostgreSQL)
    └── user_data table
        ├── user_id (UUID, Foreign Key → auth.users)
        ├── data (JSONB) - เก็บข้อมูลทั้งหมด
        ├── created_at (Timestamp)
        └── updated_at (Timestamp)
```

### Data Structure

ข้อมูลของผู้ใช้จะถูกเก็บในรูปแบบ JSONB ในตาราง `user_data`:

```json
{
  "accounts": [
    {
      "id": "uuid",
      "name": "เงินสด",
      "balance": 10000,
      "color": "#4a90e2"
    }
  ],
  "transactions": [
    {
      "id": "uuid",
      "type": "income|expense",
      "amount": 1000,
      "category": "อาหาร",
      "date": "2024-01-01",
      "accountId": "uuid",
      "note": "รายการ"
    }
  ],
  "categories": {
    "income": ["เงินเดือน", "รายได้พิเศษ", "อื่นๆ"],
    "expense": ["อาหาร", "เดินทาง", "อื่นๆ"]
  },
  "budgets": [...],
  "goals": [...],
  "challenges": [...],
  "lending": [...],
  "settings": {
    "theme": "light|dark",
    "hideBalance": false,
    "language": "th|en",
    "dailyExpenseLimit": null
  }
}
```

### Security

- **Row Level Security (RLS)**: ผู้ใช้แต่ละคนเห็นเฉพาะข้อมูลของตัวเอง
- **Supabase Auth**: จัดการ authentication และ session
- **Anon Key**: ใช้สำหรับ client-side operations (ปลอดภัยเพราะมี RLS)

### Data Flow

```
User Action
    ↓
Frontend (app.js)
    ↓
Supabase Client
    ↓
Supabase API
    ↓
PostgreSQL Database
    ↓
Response
    ↓
Update UI
```

---

## การใช้งาน

### การเข้าสู่ระบบ

1. เปิดเว็บไซต์
2. สมัครสมาชิกด้วยอีเมลและรหัสผ่าน (อย่างน้อย 6 ตัวอักษร)
3. หรือเข้าสู่ระบบด้วยบัญชีที่มีอยู่แล้ว

### การบันทึกรายการ

1. คลิกปุ่ม **เพิ่มรายรับ** หรือ **เพิ่มรายจ่าย**
2. กรอกข้อมูล: จำนวนเงิน, หมวดหมู่, วันที่, บัญชี, โน้ต
3. คลิก **บันทึก**

### การจัดการบัญชี

1. ไปที่หน้า **บัญชี**
2. คลิก **เพิ่มบัญชี** เพื่อสร้างบัญชีใหม่
3. เลือกสีและตั้งชื่อบัญชี
4. คลิก **โอนเงิน** เพื่อโอนเงินระหว่างบัญชี

### การตั้งงบประมาณ

1. ไปที่หน้า **สถิติ** หรือ **ตั้งค่า**
2. คลิก **เพิ่มงบประมาณ**
3. เลือกหมวดหมู่และตั้งจำนวนเงิน
4. ระบบจะแสดงแถบความคืบหน้าและแจ้งเตือนเมื่อใช้งบเกิน

### การตั้งเป้าหมายการออม

1. ไปที่หน้า **หน้าหลัก**
2. คลิก **ตั้งเป้าหมาย** ในส่วน "เป้าหมายการออม"
3. กรอกข้อมูล: ชื่อ, จำนวนเงิน, วันสิ้นสุด, บัญชี
4. ระบบจะแสดงความคืบหน้าและจำนวนวันคงเหลือ

### เคล็ดลับการใช้งาน

- **ซ่อนยอด**: กดปุ่ม 👁️ ที่มุมขวาบน
- **สลับภาษา**: ไปที่ **ตั้งค่า** > **ภาษา**
- **สลับธีม**: กดปุ่ม 🌓 ที่มุมขวาบน
- **ส่งออกข้อมูล**: ไปที่ **ตั้งค่า** > **ส่งออก CSV**
- **นำเข้าข้อมูล**: ไปที่ **ตั้งค่า** > **นำเข้า CSV**

---

## การแก้ไขปัญหา

### ปัญหา: "Supabase not configured"

**สาเหตุ:** ไฟล์ `config.js` ไม่ได้ตั้งค่าหรือตั้งค่าผิด

**วิธีแก้:**
1. ตรวจสอบว่าได้เพิ่ม `anonKey` ใน `config.js` แล้ว
2. ตรวจสอบว่า Supabase client library โหลดสำเร็จ (ดูใน Browser Console)
3. ตรวจสอบว่า URL และ Key ถูกต้อง

### ปัญหา: "Error creating user data"

**สาเหตุ:** ยังไม่ได้รัน SQL schema หรือ RLS policies ไม่ถูกต้อง

**วิธีแก้:**
1. ไปที่ Supabase Dashboard > SQL Editor
2. รันไฟล์ `supabase_schema.sql` อีกครั้ง
3. ตรวจสอบว่า RLS policies ถูกสร้างแล้ว
4. ดู error message ใน Browser Console เพื่อหาสาเหตุ

### ปัญหา: ไม่สามารถเข้าสู่ระบบได้

**สาเหตุ:** Email provider ไม่เปิดใช้งานหรือ email confirmation เปิดอยู่

**วิธีแก้:**
1. ไปที่ Supabase Dashboard > Authentication > Providers
2. ตรวจสอบว่า Email provider เปิดใช้งานอยู่
3. (ถ้าต้องการ) ปิด email confirmation:
   - ไปที่ Authentication > Settings > Email Auth
   - ปิด "Enable email confirmations"

### ปัญหา: ข้อมูลไม่ถูกบันทึก

**สาเหตุ:** ไม่มีสิทธิ์เข้าถึงตารางหรือ RLS policies ไม่ถูกต้อง

**วิธีแก้:**
1. ตรวจสอบว่าได้รัน SQL schema แล้ว
2. ตรวจสอบ RLS policies ใน Supabase Dashboard
3. ดู error message ใน Browser Console
4. ตรวจสอบ Network tab ว่ามี request ไปยัง Supabase หรือไม่

### ปัญหา: Deploy บน Render แล้วไม่ทำงาน

**สาเหตุ:** ยังไม่ได้รัน SQL schema หรือ config.js ไม่ถูกต้อง

**วิธีแก้:**
1. ✅ รัน SQL schema ใน Supabase Dashboard
2. ✅ ตรวจสอบว่า `config.js` มีค่าถูกต้อง
3. ✅ เปิด Browser Console (F12) ดู error message
4. ✅ ตรวจสอบ Network tab ว่าสามารถเชื่อมต่อ Supabase ได้หรือไม่

---

## อธิบายโค้ดสำคัญ

### 1. Authentication Flow

```javascript
// Constructor: ตรวจสอบ authentication status
constructor() {
    this.initSupabase();
    this.checkAuth(); // ตรวจสอบ session
}

// checkAuth: ตรวจสอบว่าผู้ใช้ login อยู่หรือไม่
async checkAuth() {
    const { data: { session } } = await this.supabase.auth.getSession();
    if (session) {
        await this.loadDataFromSupabase(); // โหลดข้อมูล
        this.showApp(); // แสดงแอป
    } else {
        this.showAuth(); // แสดงหน้า login
    }
}
```

### 2. Data Loading/Saving

```javascript
// โหลดข้อมูลจาก Supabase
async loadDataFromSupabase() {
    const { data } = await this.supabase
        .from('user_data')
        .select('data')
        .eq('user_id', this.currentUser.id)
        .single();
    this.data = data.data;
}

// บันทึกข้อมูลไปยัง Supabase
async saveDataToSupabase() {
    await this.supabase
        .from('user_data')
        .upsert({
            user_id: this.currentUser.id,
            data: this.data,
            updated_at: new Date().toISOString()
        }, { onConflict: 'user_id' });
}
```

### 3. การแปลภาษา (i18n)

```javascript
// translations object เก็บข้อความทั้งภาษาไทยและอังกฤษ
const translations = {
    th: { ... },
    en: { ... }
};

// t() method คืนข้อความตามภาษาปัจจุบัน
t(key) {
    return translations[this.currentLang]?.[key] || key;
}
```

---

## เทคโนโลยีและ Libraries

- **Supabase JS Client**: `@supabase/supabase-js@2` (CDN)
- **Canvas API**: สำหรับวาดกราฟ
- **LocalStorage**: ไม่ใช้แล้ว (ใช้ Supabase แทน)

---

## ข้อจำกัดปัจจุบัน

- ❌ ไม่มีระบบ Password Reset (ต้องทำผ่าน Supabase Dashboard)
- ❌ ไม่มีระบบ Social Login (Google, Facebook)
- ❌ การค้นหา/กรองประวัติขั้นสูงยังไม่ครบ
- ❌ ไม่มีระบบแจ้งเตือนแบบ Push Notification

---

## แผนพัฒนาในอนาคต

- [ ] เพิ่มระบบ Password Reset
- [ ] เพิ่ม Social Login (Google, Facebook)
- [ ] ตัวกรอง/ค้นหาขั้นสูง
- [ ] แจ้งเตือนงบ/เป้าหมายแบบ Push
- [ ] ปรับปรุงมินิเกมให้มีรางวัล/เลเวล
- [ ] เพิ่มระบบรายงาน (PDF Export)
- [ ] เพิ่มระบบ Budget Planning
- [ ] เพิ่มระบบ Recurring Transactions

---

## ผู้พัฒนา

สร้างด้วย ❤️ โดยใช้ Supabase และ Render

**Version:** 2.0.0  
**Last Updated:** 2025