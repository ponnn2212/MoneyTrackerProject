# Money Tracker – อธิบายโค้ดและฟีเจอร์ (ภาษาไทย)

> เว็บแอปบันทึกรายรับรายจ่ายสำหรับนักศึกษา/ผู้เริ่มต้น ทำงานบนเบราว์เซอร์ 100% ด้วย HTML, CSS และ JavaScript (ไม่มี Backend)

---

## ภาพรวมโปรเจกต์

* **วัตถุประสงค์:** ช่วยบันทึกรายรับ-รายจ่าย จัดการหลายบัญชี วางงบประมาณ ตั้งเป้าหมายออมเงิน ดูสถิติ และมีมินิเกมสร้างนิสัยการออม
* **สถาปัตยกรรม:** Single Page บนไฟล์ `index.html` + สไตล์ `styles.css` + ลอจิกทั้งหมดใน `app.js` เก็บข้อมูลด้วย `localStorage`
* **การแปลภาษา (i18n):** สลับไทย/อังกฤษได้แบบทันทีผ่านปุ่ม 🌐
* **ธีม:** รองรับ Light/Dark + โหมดซ่อนยอดเงิน

---

## โครงสร้างไฟล์

```
MoneyTrackerProject/
├─ index.html      # โครงหน้าเว็บและโมดัลต่าง ๆ
├─ styles.css      # สี ธีม เลย์เอาต์ การ์ด ปุ่ม แผนภูมิ โมดัล
├─ app.js          # แกนลอจิกทั้งหมด + การจัดการข้อมูล/วิว/กราฟ
└─ README.md       # เอกสารเดิมของโปรเจกต์
```

---

## ฟีเจอร์หลัก

1. **บันทึกรายรับ/รายจ่าย**

   * เพิ่มรายการพร้อมประเภท หมวด วันที่ โน้ต และเลือกบัญชีปลายทาง
   * อัปเดตยอดคงเหลือบัญชีแบบเรียลไทม์
2. **จัดการหลายบัญชี**

   * แสดงยอดคงเหลือรายบัญชี + โอนเงินระหว่างบัญชี (บันทึกเป็น 2 รายการ income/expense อัตโนมัติ)
3. **หมวดหมู่ & งบประมาณรายเดือน**

   * ตั้งงบต่อหมวด แถบความคืบหน้าการใช้งบ + สถานะเตือนเมื่อ 80%/100%
4. **เป้าหมายการออม (Goals)**

   * ตั้งชื่อ เป้าหมาย จำนวนเงิน วันสิ้นสุด ผูกกับบัญชี และแสดง % ความคืบหน้า + จำนวนวันคงเหลือ
5. **สถิติ & แผนภูมิ**

   * กราฟรายจ่ายตามหมวด (Bar) / รายสัปดาห์ / รายเดือน (Canvas API)
6. **ชาเลนจ์ & มินิเกม “ดวงไฟการออม”**

   * ภารกิจไม่ใช้จ่าย/ออมรายวัน + หลอดไฟสว่างตามจำนวนวันที่ออมถึงเป้า
7. **การยืม/ให้ยืม (Lending)**

   * บันทึกยอดที่ยืม/ปล่อยกู้ วันที่ครบกำหนด ยอดชำระ และสถานะ (รอ/ครบ/เกินกำหนด)
8. **ส่งออก/นำเข้า CSV**

   * แบ็กอัป/กู้คืนข้อมูลในเครื่อง
9. **ประสบการณ์ใช้งาน (UX) เพิ่มเติม**

   * ซ่อนยอดรวม (ความเป็นส่วนตัว) / ปุ่มกดด่วน / โมดัลใช้งานง่าย

---

## อธิบายโค้ดสำคัญ (Code Walkthrough)

### 1) การแปลภาษา (i18n)

* ออบเจ็กต์ `translations` เก็บข้อความ **th/en**
* เมธอด `t(key)` คืนข้อความตามภาษา ณ ปัจจุบัน
* `updateTranslations()` วิ่งหา `[data-i18n]` แล้วเติมข้อความให้ทุกจุด
* ปุ่ม `#languageToggle` เรียก `toggleLanguage()` → เซฟภาษาใน `data.settings.language` และอัปเดต UI ทั้งหมด

### 2) โมเดลข้อมูล & การเก็บข้อมูล

* คลาส `MoneyTracker` มีฟิลด์ `data` สำหรับบัญชี รายการ ธีม การตั้งค่า ฯลฯ
* `loadData()` ดึง JSON จาก `localStorage` ถ้าไม่พบ จะสร้างข้อมูลเริ่มต้น (บัญชี/ประเภทหมวด/ตั้งค่า)
* `saveData()` เซฟกลับ `localStorage`

### 3) ไหลงานหลักของแอป (Lifecycle)

* `constructor()` โหลดข้อมูล กำหนดภาษา/ธีม ผูก Events และ `updateUI()` ครั้งแรก
* `setupEventListeners()` ผูกปุ่ม: สลับภาษา/ธีม/ซ่อนยอดเงิน, เปิดโมดัลต่าง ๆ, export/import CSV, สลับแท็บ
* `updateUI()` คือศูนย์กลางเรียกย่อย: Dashboard/Accounts/Transactions/Budgets/Goals/Challenges/Lending/Charts

### 4) การบันทึกรายการ & โอนเงิน

* `addTransaction()` อ่านข้อมูลจากฟอร์ม สร้างออบเจ็กต์รายการ อัปเดตยอดบัญชี แล้วเซฟ
* `transferMoney()` ตรวจสอบยอด → ตัดจากบัญชีต้นทาง/บวกปลายทาง + สร้าง 2 รายการเพื่อให้ประวัติถูกต้อง
* ทั้งสองส่วนจะ `saveData()` และเรียก `updateUI()`

### 5) Dashboard & Recent

* `updateDashboard()` คำนวณยอดรวม รายรับ/รายจ่ายของเดือนปัจจุบัน และรายการล่าสุด 5 รายการ
* `updateBalanceDisplay()` รองรับโหมดซ่อนยอด (แสดง "฿••••")

### 6) เป้าหมาย (Goals)

* `updateGoalsList()` และ `updateGoalsListFull()` สร้างการ์ดคืบหน้า (คำนวณ % จากยอดบัญชีที่ผูก) + วันคงเหลือ

### 7) งบประมาณ (Budgets)

* `updateBudgets()` รวมรายจ่ายรายเดือนตามหมวดเทียบกับงบ → วาด Progress + สถานะแจ้งเตือน

### 8) ชาเลนจ์ & มินิเกม

* `updateChallenges()` กำหนดชาเลนจ์เริ่มต้น (ถ้าไม่มี) และแถบความคืบหน้า
* `updateMiniGame()` เลือก Goal ที่ยังไม่หมดอายุ คิด **dailyTarget ≈ target/30** และนับจำนวนวันที่ออมถึงเป้าเพื่อจุดสัญลักษณ์หลอดไฟ

### 9) Lending (ยืม/ให้ยืม)

* `updateLending()` คำนวณวันคงเหลือถึงกำหนดชำระและสถานะ (pending/completed/overdue)

### 10) กราฟ (Canvas API)

* `updateExpenseChart()` สร้างแท่งต่อหมวดของเดือนปัจจุบัน
* `updateWeeklyChart()` รวมรายจ่ายตามวันในสัปดาห์นี้
* `updateMonthlyChart()` เปรียบเทียบรายรับ/รายจ่าย 6 เดือนล่าสุด

### 11) โมดัลและประสบการณ์ใช้งาน

* `showModal(title, content)` + `closeModal()` จัดการเปิด/ปิด
* มีชุด `show*Modal()` สำหรับ: เพิ่มรายการ/บัญชี/โอนเงิน/สถิติ/เป้าหมาย/งบประมาณ/ชาเลนจ์/ยืม-ให้ยืม/ตั้งค่า

---

## วิธีเริ่มใช้งาน

1. เปิดไฟล์ `index.html` ด้วยเบราว์เซอร์ (Chrome แนะนำ)
2. กดปุ่ม ➕ เพื่อเพิ่มรายการแรก
3. ทดลองสลับภาษา/ธีม และตั้งค่าเป้าหมาย/งบประมาณตามต้องการ

> **หมายเหตุ:** ข้อมูลเก็บในเครื่อง (localStorage) หากล้างข้อมูลเบราว์เซอร์ ข้อมูลจะหาย ควรส่งออก CSV เป็นระยะ

---

## เคล็ดลัดการใช้งาน

* ซ่อนยอด: กดปุ่ม 👁️ ที่มุมขวาบนของแอป
* โอนเงิน: เปิด **บัญชี** → ฟอร์ม "โอนเงินระหว่างบัญชี"
* ดูแผนภูมิแบบเต็ม: เปิด **สถิติ** (มีแท็บรายสัปดาห์/รายเดือน)
* มินิเกม: ไปที่ **ชาเลนจ์** แล้วเริ่มออมตามเป้าหมายรายวัน

---

## การปรับแต่ง/ต่อยอด

* เพิ่มหมวดหมู่: ปรับ `data.categories` ใน `loadData()` หรือเพิ่ม UI จัดการหมวด
* สูตรมินิเกม: เปลี่ยนกฎนับวัน/เป้าใน `updateMiniGame()`
* การแจ้งเตือน: ผูก Web Notifications/Push (ต้องมี Service Worker และ Backend ถ้าจะซิงก์หลายอุปกรณ์)
* ระบบล็อกอิน/ซิงก์: สร้าง API + Auth (เช่น OAuth/Email) แล้วแทน `localStorage` ด้วย REST/GraphQL

---

## ข้อจำกัดปัจจุบัน

* ไม่มีระบบผู้ใช้/ล็อกอินและการซิงก์ข้ามอุปกรณ์
* การค้นหา/กรองประวัติขั้นสูงยังไม่ครบ
* การตรวจสอบความถูกต้องของฟอร์มมีพื้นฐาน ควรเสริม edge cases (เช่น จำนวนเงินติดลบ/วันที่ผิดรูปแบบ)

---

## แผนพัฒนา (Roadmap แนะนำ)

* [ ] ล็อกอิน (Google/อีเมล) + โปรไฟล์ผู้ใช้
* [ ] ซิงก์ Cloud + สำรองอัตโนมัติ
* [ ] ตัวกรอง/ค้นหาขั้นสูง และแท็ก
* [ ] แจ้งเตือนงบ/เป้าหมายแบบ Push
* [ ] ปรับปรุงมินิเกม/ชาเลนจ์ให้มีรางวัล/เลเวล
* [ ] ทดสอบหน่วย (Unit test) บนเมธอดสำคัญ เช่น addTransaction/transferMoney

---

## เทคนิคโค้ดที่ควรรู้

* ใช้ `setTimeout(..., 100)` หลังสลับวิวเพื่อรอ DOM พร้อมก่อนวาดกราฟ (หลีกเลี่ยงขนาด canvas เพี้ยน)
* รีเซ็ตฟอร์ม + ตั้งค่าวันที่ปัจจุบันทุกครั้งหลังบันทึกรายการ (`setupDateInput()`)
* ใช้คลาส/เมธอดรวมศูนย์ (OOP) ช่วยให้ดูแลง่ายและขยายฟีเจอร์สะดวก

---

## ใบอนุญาต

> โปรเจกต์นี้จัดทำเพื่อการศึกษา/ฝึกฝน สามารถปรับใช้ส่วนตัวได้ตามสมควร
